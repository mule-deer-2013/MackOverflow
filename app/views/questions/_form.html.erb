<%= form_for @question do |f| %>
  <%= f.label :question %>
  <%= f.text_area :content %>
  <p>
  <%= f.label :tag_list %>
  <%= f.text_field :tag_list %>
</p>
  <%= f.submit "Ask it!" %>
  <% end %>
